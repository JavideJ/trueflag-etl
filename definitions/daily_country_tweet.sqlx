config {
type: "table",
schema: "tweet",
tags: ["tweet"],
description: "Tabla diaria con número de tweets agrupados por país."
}

SELECT
DATE(date) AS tweet_date,
user.location.country AS country,
COUNT(DISTINCT id) AS tweet_count
FROM
trueflag-459814.tweet.raw_tweet
GROUP BY
tweet_date,
country